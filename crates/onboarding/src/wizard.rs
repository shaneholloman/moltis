/// Run the interactive onboarding wizard.
pub async fn run_onboarding() -> anyhow::Result<()> {
    todo!(
        "detect existing config → CLI install → daemon → model auth → channels → skills → start → test"
    )
}
